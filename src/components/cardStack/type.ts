import { IconType } from "react-icons";

export type CardType = {
  badge?: string;
  BadgeIcon?: IconType;
  title: string;
  background: string;
  colorBadge: string;
  colorItens: string;
  items: string[];
};
